# 安全策略

Winflow 处理的是个人本机活动数据，因此安全和隐私边界非常重要。

## 支持范围

当前项目仍处于早期阶段。安全修复会优先覆盖主分支上的最新代码。

## 报告安全问题

如果你发现可能导致本地活动数据泄露、未授权上传、路径泄露或敏感信息输出的问题，请不要先公开利用细节。建议先开一个不含敏感细节的 issue，说明你希望私下沟通安全问题。

## 安全设计原则

- 默认本地保存；
- 上传必须显式配置服务器和 token；
- 应用统计接口不输出窗口标题、exe 路径或完整 URL；
- 浏览器默认只按应用统计，不按标签页或站点统计；
- 本地数据库、日志、图标缓存和 `.env` 默认不提交到 Git。
