# Roadmap

Winflow 的目标不是复刻 macOS Dayflow 的实现，而是在 Windows 上建立一个 Windows-first、local-first 的自动工作日志。Dayflow 只作为产品体验与开源成熟度参考，不作为技术栈参考。

## P0：开源发布基线

- [x] 应用使用统计接口不输出窗口标题、exe 路径、完整 URL；
- [x] 默认不读取浏览器历史；
- [x] 默认不上传；
- [x] 忽略本地数据库、日志、图标缓存和 `.env`；
- [x] 增加 README、隐私、安全、贡献、CI 等开源基础文件；
- [x] 增加发布审计和打包审计，拦截本机私有产物、macOS 技术栈残留和安装态路径回退。

## P1：Windows 本地体验

- [x] 拆分 `app/main.py`，分离 CLI、Web、采集器；
- [x] 文档化上传 API；
- [x] 安装态基础路径审计：内置静态图标随包分发，数据库和 exe 图标缓存写入用户数据目录；
- [ ] 将 `app/web.py` 中的 HTML/CSS/JS 继续拆到 `static/` 或模板文件；
- [ ] 增加系统托盘入口；
- [ ] 增加 Windows 自启动配置；
- [ ] 增强采集器健康状态与错误提示；
- [ ] 增加数据保留期和自动清理。

## P2：统计和时间线成熟化

- [ ] 更稳定的应用名称归一化；
- [ ] 更清晰的活动带可视化；
- [ ] 更好的空闲/锁屏识别；
- [ ] 周报、日报、应用趋势；
- [ ] 可导出的 JSON/Markdown schema。

## P3：可选同步和生态

- [ ] 提供最小服务端示例；
- [ ] 支持多设备 device_id；
- [ ] 支持本地-only 的摘要实验；
- [ ] 在不破坏隐私默认值的前提下探索插件机制。
